import React from "react";

// dropdown which will feature some list of project/experience cards, populating with information from a json
// pass in boolean for project/experience
// read from json and create experience or project cards, depending on bool type 

export default function Dropdown(type : boolean){

    return (
        <div>
            dropdown
        </div>
    )
}